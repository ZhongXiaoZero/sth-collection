正则表达式小记：
例1：/the/gi
例2：/\d+/g

/   是这个正则表达式的头部；
the 是我们想要匹配的模式；
/   是这个正则表达式的尾部；
g   代表着 global(全局)，意味着返回所有的匹配而不仅仅是第一个；
i   代表着忽略大小写，意思是当我们寻找匹配的字符串的时候忽略掉字母的大小写；
\d  用来获取一个字符串的数字；
+   允许正则表达式匹配一个或更多数字；
\s  查找一个字符串中的空白,空白字符有 " " (空格符)、\r (回车符)、\n (换行符)、\t (制表符) 和 \f (换页符)。